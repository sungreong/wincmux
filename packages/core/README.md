# Core Modules

- `packages/core/`: active core runtime (Node/TypeScript, no dotnet required)
- `legacy-dotnet/core/`: previous .NET core implementation for reference only

Active core run command:

```powershell
npm --workspace @wincmux/core run dev
```
